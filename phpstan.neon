includes:
    - vendor/larastan/larastan/extension.neon
    - phpstan-baseline.neon

parameters:
    level: 5

    paths:
        - app

    # Reportar errores no cubiertos por el baseline como fallo de CI.
    reportUnmatchedIgnoredErrors: true
